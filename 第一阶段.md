可以，当然能拆细。你这份「阶段一」其实不是一个阶段，而是一个**小型企业 AI 后端项目**了。老八建议把它拆成 **8 个小模块、28 个小任务**，每个任务都能单独验收，这样你每天做一点，不会被“我要学 FastAPI”这种大词压死。

下面我按“从能跑 → 能接模型 → 能存数据 → 能上传文档 → 能流式 → 能鉴权 → 能部署”的顺序拆。

---

# 阶段一：FastAPI 工程底座细化版

## 总目标

做出一个最小但完整的 AI 应用后端，最终具备：

```
健康检查
普通聊天
模型调用日志
聊天历史
文档上传
文档列表
文档解析
知识库问答入口
API Key 鉴权
Docker 部署
README 文档
```

最终项目大概长这样：

```
ai-backend/
├── app/
│   ├── main.py
│   ├── api/
│   │   ├── health.py
│   │   ├── chat.py
│   │   ├── documents.py
│   │   ├── knowledge.py
│   │   └── sessions.py
│   ├── core/
│   │   ├── config.py
│   │   ├── security.py
│   │   └── logging.py
│   ├── models/
│   │   ├── document.py
│   │   ├── chat_message.py
│   │   └── llm_log.py
│   ├── schemas/
│   │   ├── chat.py
│   │   ├── document.py
│   │   └── knowledge.py
│   ├── services/
│   │   ├── llm.py
│   │   ├── document_parser.py
│   │   └── knowledge.py
│   ├── db/
│   │   ├── session.py
│   │   └── init_db.py
│   └── utils/
│       └── time.py
├── uploads/
├── tests/
├── .env
├── .env.example
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
└── README.md
```

---

# 模块 0：环境和项目骨架

这一块的目标不是学高级东西，只是先让项目能跑。

## 0.1 创建项目目录

你要掌握：

```
Python 项目结构
虚拟环境
requirements.txt
FastAPI 基本启动方式
```

任务：

- [x]  创建 `ai-backend/`
- [x]  创建 `app/main.py`
- [x]  创建 `requirements.txt`
- [x]  创建 `.env`
- [x]  创建 `README.md`
- [x]  能用 `uvicorn app.main:app --reload` 启动

建议依赖先放这些：

```
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
httpx
sqlmodel
psycopg2-binary
python-multipart
```

先不用一口气装太多，不然报错会让人烦躁。

---

## 0.2 写第一个 FastAPI 应用

目标：打开 `/docs` 能看到接口文档。

你要掌握：

```python
from fastapi import FastAPI

app = FastAPI()
```

验收：

- [x]  浏览器打开 `http://127.0.0.1:8000/docs`
- [x]  能看到 Swagger 文档
- [x]  终端没有报错

---

# 模块 1：基础接口

这一块是 FastAPI 的基础语法区。

## 1.1 GET /health 健康检查

目标：做一个最简单的服务状态接口。

接口：

```
GET /health
```

返回：

```json
{
  "status": "ok",
  "service": "ai-backend"
}
```

你要掌握：

```
GET 接口
路径设计
JSON 返回
```

验收：

- [x]  浏览器访问 `/health` 返回正常
- [x]  `/docs` 里能看到这个接口
- [x]  curl 能调用成功

示例：

```bash
curl http://127.0.0.1:8000/health
```

---

## 1.2 路径参数：GET /items/{item_id}

这个接口只是练手，不一定放最终项目里。

目标：理解路径参数。

接口：

```
GET /items/{item_id}
```

示例：

```
GET /items/123
```

返回：

```json
{
  "item_id": 123
}
```

你要掌握：

```python
@app.get("/items/{item_id}")
def get_item(item_id: int):
    ...
```

验收：

- [x]  输入 `/items/123` 正常
- [x]  输入 `/items/abc` 自动报参数错误
- [x]  理解 FastAPI 会自动做类型校验

---

## 1.3 查询参数：GET /search?q=xxx&limit=10

目标：理解 query 参数。

接口：

```
GET /search?q=python&limit=10
```

返回：

```json
{
  "q": "python",
  "limit": 10
}
```

你要掌握：

```python
def search(q: str, limit: int = 10):
    ...
```

验收：

- [x]  不传 `limit` 时默认是 10
- [x]  传 `limit=5` 时返回 5
- [x]  传错误类型时 FastAPI 自动报错

---

## 1.4 POST 请求体：POST /echo

目标：理解 Pydantic 请求体。

接口：

```
POST /echo
```

请求体：

```json
{
  "message": "你好"
}
```

返回：

```json
{
  "message": "你好"
}
```

你要掌握：

```python
from pydantic import BaseModel

class EchoRequest(BaseModel):
    message: str
```

验收：

- [x]  用 `/docs` 能直接测试
- [x]  少传 `message` 会报校验错误
- [x]  知道请求体不是随便写 dict，而是用 Pydantic 定义

---

# 模块 2：配置和环境变量

这一块非常重要，因为后面模型 API Key、数据库地址都不能写死在代码里。

## 2.1 创建 .env 配置

目标：把配置从代码里拿出来。

`.env` 示例：

```
APP_NAME=ai-backend
ENV=dev

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=你的key
OPENAI_MODEL=gpt-4o-mini

DATABASE_URL=postgresql://postgres:password@localhost:5432/ai_backend
API_KEY=dev-secret-key
```

你要掌握：

```
.env 是什么
为什么不能把 key 写死
什么是环境变量
```

验收：

- [x]  `.env` 存在
- [x]  `.env.example` 存在，但不包含真实 key
- [x]  `.gitignore` 里忽略 `.env`

---

## 2.2 创建 config.py

目标：统一读取配置。

文件：

```
app/core/config.py
```

负责读取：

```
APP_NAME
OPENAI_BASE_URL
OPENAI_API_KEY
OPENAI_MODEL
DATABASE_URL
API_KEY
```

你要掌握：

```python
from pydantic_settings import BaseSettings
```

验收：

- [x]  应用启动时能读取配置
- [x]  没有 key 时能给出明确报错
- [x]  代码里不再直接写 API Key

---

# 模块 3：普通聊天接口 /chat

这是第一阶段最核心的小闭环。

## 3.1 定义 ChatRequest 和 ChatResponse

目标：先不接模型，先把接口结构定下来。

接口：

```
POST /chat
```

请求：

```json
{
  "message": "你好",
  "session_id": "demo-session",
  "temperature": 0.7,
  "max_tokens": 1024
}
```

返回：

```json
{
  "session_id": "demo-session",
  "message": "你好，我是 AI 助手",
  "model": "gpt-4o-mini",
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 20,
    "total_tokens": 30
  }
}
```

你要掌握：

```
Pydantic 请求模型
Pydantic 响应模型
response_model
Optional 字段
默认值
```

验收：

- [x]  `/chat` 可以先返回假数据
- [x]  请求体校验正常
- [x]  response_model 生效

---

## 3.2 封装 LLM 服务

目标：不要在接口函数里直接写模型调用代码。

文件：

```
app/services/llm.py
```

它负责：

```
接收用户 message
调用 OpenAI-compatible API
返回模型回复、usage、model
处理异常
处理超时
```

你要掌握：

```
服务层 service 是什么
为什么 API 层不要写太多业务逻辑
httpx.AsyncClient
OpenAI-compatible 接口格式
```

OpenAI-compatible 请求大概是：

```json
{
  "model": "xxx",
  "messages": [
    {
      "role": "user",
      "content": "你好"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 1024
}
```

验收：

- [x]  `llm.py` 可以独立调用模型
- [x]  超时会报清楚的错误
- [x]  API Key 错误时不会导致服务崩溃
- [x]  返回里有 `content` 和 `usage`

---

## 3.3 /chat 接入真实大模型

目标：从假回复变成真实模型回复。

流程：

```
用户请求 /chat
↓
chat.py 接收请求
↓
调用 services/llm.py
↓
llm.py 请求模型 API
↓
返回回复给用户
```

验收：

- [x]  `/chat` 可以真实返回模型回答
- [x]  可以调整 `temperature`
- [x]  可以调整 `max_tokens`
- [x]  模型调用失败时返回合理错误，而不是一串大 traceback

---

## 3.4 错误处理：HTTPException

目标：让接口错误更像正式项目。

常见错误：

```
模型 API Key 错误
模型超时
请求内容为空
max_tokens 太大
temperature 超范围
```

你要掌握：

```python
from fastapi import HTTPException
```

验收：

- [x]  message 为空时返回 400
- [x]  模型超时时返回 504
- [x]  模型服务异常时返回 502
- [x]  错误返回格式清楚

---

# 模块 4：数据库 CRUD

这一块是从“玩具接口”变成“工程项目”的关键。

建议先用 PostgreSQL。如果你后面要接 pgvector，PostgreSQL 更顺手。

---

## 4.1 设计三张表

先不要设计太复杂，够用就行。

### documents 表

存上传文档的元数据。

字段：

```
id
user_id
filename
file_path
content_type
size
status
created_at
```

---

### chat_messages 表

存聊天记录。

字段：

```
id
session_id
user_id
role
content
created_at
```

role 可以是：

```
user
assistant
system
tool
```

---

### llm_logs 表

存模型调用日志。

字段：

```
id
user_id
session_id
model
prompt_tokens
completion_tokens
total_tokens
latency_ms
success
error_message
created_at
```

你要掌握：

```
为什么要存聊天记录
为什么要存模型调用日志
企业里为什么要审计和追踪成本
```

验收：

- [x]  你能说清楚每张表干什么
- [x]  你能说清楚为什么不是只存最终回答

---

## 4.2 创建数据库连接

文件：

```
app/db/session.py
```

目标：

```
读取 DATABASE_URL
创建 engine
提供 get_session 依赖
```

你要掌握：

```
SQLModel / SQLAlchemy
engine
session
dependency injection
```

验收：

- [x]  应用能连接 PostgreSQL
- [x]  数据库连接失败时能看到明确错误
- [x]  `/docs` 正常打开

---

## 4.3 创建 Model 文件

文件：

```
app/models/document.py
app/models/chat_message.py
app/models/llm_log.py
```

目标：把三张表用 Python 类表示出来。

你要掌握：

```python
from sqlmodel import SQLModel, Field
```

验收：

- [x]  三个模型类创建完成
- [x]  字段类型正确
- [x]  有 created_at
- [x]  能自动建表，或者能通过初始化脚本建表

---

## 4.4 保存聊天记录

目标：调用 `/chat` 时，不只是返回结果，还要存入数据库。

流程：

```
收到用户消息
↓
保存 user message/
↓
调用模型
↓
保存 assistant message
↓
返回结果
```

验收：

- [x]  调用一次 `/chat`
- [x]  数据库里出现一条 user 消息
- [x]  数据库里出现一条 assistant 消息
- [x]  session_id 相同

---

## 4.5 保存模型调用日志

目标：记录每次模型调用的成本和状态。

要记录：

```
model
prompt_tokens
completion_tokens
total_tokens
latency_ms
success
error_message
```

验收：

- [x]  成功调用模型时有一条 success=true 日志
- [x]  模型调用失败时有一条 success=false 日志
- [x]  能看到耗时
- [x]  能看到 token usage，如果模型返回了的话

---

# 模块 5：聊天历史接口

## 5.1 GET /sessions/{session_id}/messages

目标：根据 session_id 查聊天历史。

接口：

```
GET /sessions/demo-session/messages
```

返回：

```json
{
  "session_id": "demo-session",
  "messages": [
    {
      "role": "user",
      "content": "你好",
      "created_at": "2026-05-06T10:00:00"
    },
    {
      "role": "assistant",
      "content": "你好，有什么可以帮你？",
      "created_at": "2026-05-06T10:00:01"
    }
  ]
}
```

你要掌握：

```
路径参数
数据库查询
按时间排序
response_model
```

验收：

- [x]  调用 `/chat` 后能查到历史
- [x]  消息按时间顺序排列
- [x]  查不存在的 session_id 时返回空列表或 404，二选一即可

老八建议初期返回空列表就行，别搞太复杂。

---

# 模块 6：文件上传与文档管理

这是为第二阶段 RAG 做准备。

---

## 6.1 POST /documents/upload

目标：支持上传文件。

接口：

```
POST /documents/upload
```

支持：

```
PDF
Word
Excel
TXT
```

初期可以先只支持 TXT，后面再加 PDF/Word/Excel。

你要掌握：

```python
from fastapi import UploadFile, File
```

验收：

- [x]  能上传 TXT 文件
- [x]  文件保存到 `uploads/`
- [x]  数据库 documents 表里有记录
- [x]  返回 document_id

返回示例：

```json
{
  "document_id": 1,
  "filename": "test.txt",
  "status": "uploaded"
}
```

---

## 6.2 文件类型校验

目标：不能什么文件都让用户传。

允许类型：

```
.txt
.pdf
.docx
.xlsx
```

验收：

- [ ]  上传 `.txt` 成功
- [ ]  上传 `.exe` 失败
- [ ]  文件过大时失败，比如超过 20MB

---

## 6.3 解析 TXT

目标：上传后能提取文本内容。

流程：

```
上传文件
↓
保存文件
↓
读取文本
↓
保存解析状态
```

可以先在 documents 表里加：

```
text_content
```

虽然后面正式项目可能不会这么干，但学习阶段这样最直观。

验收：

- [ ]  上传 TXT 后能读取里面的内容
- [ ]  数据库里能看到 text_content
- [ ]  中文不会乱码

---

## 6.4 解析 PDF / Word / Excel

这个可以后置，不用第一天做。

推荐顺序：

```
TXT → PDF → Word → Excel
```

建议库：

```
TXT：Python 原生 open
PDF：pypdf 或 pymupdf
Word：python-docx
Excel：openpyxl 或 pandas
```

验收：

- [ ]  PDF 能提取文本
- [ ]  Word 能提取段落
- [ ]  Excel 能提取 sheet 内容
- [ ]  解析失败时 status 变成 failed

---

## 6.5 GET /documents

目标：查看自己上传过哪些文档。

接口：

```
GET /documents
```

返回：

```json
[
  {
    "id": 1,
    "filename": "制度.pdf",
    "status": "parsed",
    "created_at": "2026-05-06T10:00:00"
  }
]
```

你要掌握：

```
数据库列表查询
分页 limit/offset
按时间倒序
```

验收：

- [ ]  上传后能在列表看到
- [ ]  支持 `limit`
- [ ]  支持 `offset`
- [ ]  不返回太大的 text_content

---

# 模块 7：知识库问答入口

注意，这里不是完整 RAG，只是给阶段二留接口。

阶段一只需要做一个“弱版本”。

---

## 7.1 POST /knowledge/query

目标：先实现一个简化版文档问答。

请求：

```json
{
  "question": "这个文档讲了什么？",
  "document_id": 1
}
```

流程：

```
根据 document_id 找到 text_content
↓
把文档内容和问题拼进 prompt
↓
调用大模型
↓
返回答案
```

这个版本不做 embedding、不做向量库，只做最小闭环。

返回：

```json
{
  "answer": "这个文档主要讲了……",
  "sources": [
    {
      "document_id": 1,
      "filename": "test.txt"
    }
  ]
}
```

你要掌握：

```
RAG 的最朴素形态
为什么长文档不能一直这样塞 prompt
为什么第二阶段要 chunk + embedding
```

验收：

- [ ]  上传一个 TXT
- [ ]  调 `/knowledge/query` 能根据文档回答
- [ ]  返回来源文档
- [ ]  文档不存在时返回 404

---

# 模块 8：异步、流式和超时

这一块建议等普通 `/chat` 稳定后再做。

---

## 8.1 async/await

目标：理解为什么 Web 后端常用异步。

你要掌握：

```python
async def
await
httpx.AsyncClient
```

验收：

- [ ]  `/chat` 是 async 接口
- [ ]  模型调用用 httpx async
- [ ]  多个请求不会互相严重阻塞

---

## 8.2 超时控制

目标：模型卡住时，后端不能一直等。

配置：

```
LLM_TIMEOUT_SECONDS=60
```

验收：

- [ ]  模型超过 60 秒返回错误
- [ ]  错误日志里记录 timeout
- [ ]  用户收到清楚提示

---

## 8.3 重试机制

目标：对临时网络错误重试。

建议：

```
最多重试 2 次
只对网络错误和 5xx 重试
不要对 401/403 重试
```

验收：

- [ ]  网络临时失败会重试
- [ ]  API Key 错误不会傻傻重试
- [ ]  日志里能看到 retry 次数

---

## 8.4 StreamingResponse / SSE

目标：让 `/chat` 支持流式输出。

可以新增接口：

```
POST /chat/stream
```

你要掌握：

```
StreamingResponse
Server-Sent Events
流式模型接口
```

验收：

- [ ]  客户端能一段一段收到回答
- [ ]  前端或 curl 能看到流式效果
- [ ]  模型结束时连接正常关闭

这个不是阶段一早期必须做，可以排在后面。

---

# 模块 9：基础鉴权

这一块很重要，因为企业 AI 应用不能裸奔。

---

## 9.1 API Key 鉴权

目标：请求必须带 API Key。

请求头：

```
X-API-Key: dev-secret-key
```

你要掌握：

```python
from fastapi import Header, Depends
```

验收：

- [ ]  不带 key 返回 401
- [ ]  key 错误返回 401
- [ ]  key 正确才能访问 `/chat`
- [ ]  `/health` 可以不鉴权

---

## 9.2 用户隔离

初期可以简单一点，不做完整登录系统。

可以约定：

```
X-User-Id: user-001
```

然后所有数据都带上：

```
user_id
```

验收：

- [ ]  A 用户上传的文档，B 用户看不到
- [ ]  A 用户的聊天历史，B 用户查不到
- [ ]  查询文档时必须校验 user_id

这个是企业项目里非常核心的意识：**不是能查就行，还要查得对、查得安全。**

---

# 模块 10：日志

日志不是“锦上添花”，企业项目里它是救命的。

---

## 10.1 基础访问日志

目标：知道谁调用了什么接口。

记录：

```
请求路径
请求方法
状态码
耗时
```

验收：

- [ ]  每次请求终端能看到日志
- [ ]  出错时能看到错误原因
- [ ]  不打印 API Key

---

## 10.2 模型调用日志

前面数据库已经有 `llm_logs`，这里补验收。

记录：

```
用户
session_id
model
耗时
token
是否成功
错误原因
```

验收：

- [ ]  成功调用有日志
- [ ]  失败调用也有日志
- [ ]  能用 SQL 查当天调用次数
- [ ]  能估算 token 成本

---

# 模块 11：Docker 部署

等本地跑通后再做，不要一上来就 Docker。

---

## 11.1 Dockerfile

目标：把 FastAPI 应用打包成镜像。

你要掌握：

```
Dockerfile
工作目录
安装 requirements
启动 uvicorn
```

验收：

- [ ]  `docker build` 成功
- [ ]  `docker run` 后能访问 `/health`
- [ ]  容器日志能看到启动信息

---

## 11.2 docker-compose.yml

目标：一键启动 API + PostgreSQL。

服务：

```
api
postgres
```

验收：

- [ ]  `docker compose up -d` 成功
- [ ]  API 能连上数据库
- [ ]  数据能持久化
- [ ]  停掉再启动数据还在

---

## 11.3 Nginx 反代

目标：部署后外部通过固定端口访问。

本地学习阶段可以先不做；云服务器部署时再做。

你要掌握：

```
反向代理
端口映射
后端服务
```

验收：

- [ ]  Nginx 可以转发到 FastAPI
- [ ]  `/docs` 能打开
- [ ]  `/chat` 能正常调用
- [ ]  日志能看

---

# 模块 12：README 和接口文档

这个别忽略。你以后入职做企业项目，README 写得清楚会非常加分。

---

## 12.1 README 最少写这些

内容：

```
项目简介
功能列表
环境变量说明
本地启动方式
Docker 启动方式
接口调用示例
数据库表说明
常见问题
```

验收：

- [ ]  新电脑按 README 能启动
- [ ]  curl 示例能直接复制用
- [ ]  没有真实 API Key
- [ ]  说明哪些功能已完成，哪些还没完成

---

# 推荐实际开发顺序

你不要按知识点学，按“能交付的小闭环”学。

## 第 1 轮：最小可运行

目标：后端能启动。

- [ ]  创建项目
- [ ]  安装 FastAPI
- [ ]  写 `main.py`
- [ ]  实现 `/health`
- [ ]  打开 `/docs`

完成后你已经不是“在看教程”，而是在做项目了。

---

## 第 2 轮：聊天接口假数据版

目标：先理解 POST 和 Pydantic。

- [ ]  创建 `ChatRequest`
- [ ]  创建 `ChatResponse`
- [ ]  实现 `/chat`
- [ ]  返回假回复
- [ ]  用 `/docs` 测试

---

## 第 3 轮：接入真实模型

目标：AI 能力服务化。

- [ ]  配置 `.env`
- [ ]  创建 `config.py`
- [ ]  创建 `services/llm.py`
- [ ]  用 httpx 调 OpenAI-compatible API
- [ ]  `/chat` 返回真实模型回复
- [ ]  处理超时和错误

---

## 第 4 轮：加数据库

目标：能保存数据。

- [ ]  设计三张表
- [ ]  配置 PostgreSQL
- [ ]  创建数据库连接
- [ ]  创建 SQLModel 模型
- [ ]  保存聊天记录
- [ ]  保存模型日志

---

## 第 5 轮：聊天历史

目标：能查历史。

- [ ]  实现 `GET /sessions/{session_id}/messages`
- [ ]  根据 session_id 查询
- [ ]  按时间排序
- [ ]  做 user_id 隔离

---

## 第 6 轮：文件上传

目标：为 RAG 做准备。

- [ ]  实现上传 TXT
- [ ]  保存文件到 uploads
- [ ]  保存文档元数据
- [ ]  解析 TXT 内容
- [ ]  实现文档列表

---

## 第 7 轮：简化知识库问答

目标：先做没有向量库的朴素 RAG。

- [ ]  根据 document_id 找文本
- [ ]  拼 prompt
- [ ]  调模型回答
- [ ]  返回 sources
- [ ]  文档不存在时返回 404

---

## 第 8 轮：鉴权和部署

目标：从 demo 变成可交付。

- [ ]  API Key 鉴权
- [ ]  X-User-Id 简单用户隔离
- [ ]  Dockerfile
- [ ]  docker-compose
- [ ]  README

---

# 每天怎么学比较合适

如果你时间不多，我建议按这个节奏：

## Day 1：FastAPI 入门

完成：

- `/health`
- `/docs`
- 路径参数
- 查询参数
- POST 请求体

不要看太多视频，写出来就行。

---

## Day 2：/chat 假数据版

完成：

- `ChatRequest`
- `ChatResponse`
- `/chat`
- response_model
- HTTPException

---

## Day 3：接大模型

完成：

- `.env`
- `config.py`
- `llm.py`
- httpx 调模型
- `/chat` 真实返回

---

## Day 4：数据库

完成：

- PostgreSQL 连接
- 三张表
- 建表
- 保存聊天记录

---

## Day 5：日志

完成：

- 保存 LLM 调用日志
- 记录耗时
- 记录 usage
- 记录错误

---

## Day 6：文件上传

完成：

- 上传 TXT
- 保存文件
- 保存 documents 表
- 解析文本

---

## Day 7：文档列表和简化问答

完成：

- `GET /documents`
- `POST /knowledge/query`
- 用全文拼 prompt 先跑通

---

## Day 8：鉴权

完成：

- API Key
- X-User-Id
- 数据隔离

---

## Day 9：Docker

完成：

- Dockerfile
- docker-compose
- README 启动说明

---

# 第一阶段最终验收标准

你最后要能演示这个流程：

```
1. 启动后端
2. 访问 GET /health，返回 ok
3. 调用 POST /chat，AI 正常回复
4. 数据库里能看到聊天记录
5. 数据库里能看到模型调用日志
6. 上传一个 txt 文档
7. GET /documents 能看到文档
8. POST /knowledge/query 能基于文档回答
9. 不带 API Key 会被拒绝
10. Docker compose 能启动整个服务
```

如果这 10 个能跑通，阶段一就很扎实了。

---

# 你现在先别急着做全部

你现在的第一步应该只做这个：

## 小任务 1：跑通最小 FastAPI

只需要完成：

```
创建项目
安装依赖
写 main.py
实现 GET /health
打开 /docs
```

代码大概只有十几行。

等这个跑通，再加 `/chat`。

不要一上来就数据库、Docker、RAG 一起上，不然会变成“报错修罗场”，你会烦，老八也会跟着小声骂两句……当然是骂 Docker，不骂你。